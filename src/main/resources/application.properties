jwt.secret=${JWT_SECRET:javainuse}
spring.datasource.url=jdbc:postgresql://${DB_HOST:57.128.201.126}:${DB_PORT:5432}/${DB_NAME:bavarians}
#spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:bavarians}

spring.datasource.username=${DB_USERNAME:bavuser}
spring.datasource.password=${DB_PASSWORD}

#spring.jpa.hibernate.ddl-auto=create-drop
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation= true

spring.thymeleaf.template-loader-path: classpath:/templates
spring.thymeleaf.suffix: .html
spring.thymeleaf.cache: false

#server.servlet.context-path=/

spring.messages.basename=validation
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=true

graphql.servlet.mapping: /api/graphql
graphql.tools.schema-location-pattern=**/*.graphlqls
#spring.graphql.schema.locations=classpath:graphql/

server.session.cookie.max-age=864000
server.session.timeout=864000
server.servlet.session.timeout=864000
# to nie ten propertas
spring.session.timeout=24h
#server.servlet.session.persistent=true
#spring.session.store-type=jdbc

server.port=${SERVER_PORT:8080}
pdf.temp.directory=./pdfs
management.endpoints.web.exposure.include=health,info
management.endpoint.health.probes.enabled=true
management.endpoint.health.show-details=always
management.health.defaults.enabled=true

# Debug logging for authentication
logging.level.com.bavarians.service=DEBUG
logging.level.org.springframework.security=DEBUG